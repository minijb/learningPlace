# 跨章节实践项目

> 本文件与 [Topic.md](./Topic.md)、[Stages.md](./Stages.md) 构成三方视图：**[Topic.md](./Topic.md) 是大纲**（各节要点与边界声明），**[Stages.md](./Stages.md) 是按节推进的执行细节**（每节自包含的阶段序列：目标/知识点/任务/检验标准），**本文件是按项目对齐的里程碑视图**——把散在各节阶段里的实践任务按项目串成演进链。
> **使用方法**：按项目推进时查其里程碑表；「来源」列的"章节·阶段"可回跳 [Stages.md](./Stages.md) 对应阶段获取执行细节与检验标准，「前置」列给出相对先后与依赖。里程碑编号（M0、M1…）只在项目内递增，跨项目引用写作 P1·M4。
> **时长说明**：[Stages.md](./Stages.md) 各阶段标注的"约 X–Y 周"仅为相对量级参考，本文件一律不重复标注；所有里程碑只表达相对先后与前置依赖，不绑定绝对时长与日历。
> **起步建议双线先行**：1·阶段1–2 与 2·阶段1 并行起步；其中 1·阶段2 的「渲染器 RAII 化」任务自然落在 P1·M1 之后（见 P1·M2）。

---

## 项目 P1：自写渲染器（贯穿主线资产）

**定位与最终形态**：第 3 节起步、各节共同演进的贯穿性核心资产——从窗口与三角形开始，最终形态为「一套渲染器、双 API 后端（OpenGL + Vulkan）、GPU 驱动特性齐备」：一份场景描述即可驱动模型/光照/阴影/后处理的完整画面，2D 小引擎（P2）运行其上，GPU 特性集（P4）并入其中。

**参与章节**：第 3 节（主体）、第 1 节（RAII/工程化/并发/性能）、第 4 节（接口抽象）、第 5 节（compute 并入）、第 6 节（Vulkan 复刻）；第 2 节不改动它，但以它冻结的基准场景为对照对象（经 P3）。

**里程碑表**：

| 编号 | 里程碑 | 达成内容 | 来源（章节·阶段） | 前置 |
|---|---|---|---|---|
| M0 | 🧱 骨架就位 | CMake 骨架工程 + Shader 封装（报错可读、支持重载）+ 调试输出与 RenderDoc 首次抓帧 + 首个三角形；确立「教程 demo 验证 → 沉淀进渲染器」双线工作流 | 3·阶段1（起步：窗口、首个三角形与可调试环境） | — |
| M1 | 🎥 基础可用 | 纹理物体 + 自由相机 + 深度正确的完整画面，一份场景描述即可驱动；补 2D quad 路径（P2 的渲染地基） | 3·阶段2（变换、相机与纹理：渲染器基础可用） | M0 |
| M2 | 🔒 RAII 化 | Texture/Shader/Mesh/FBO 全部收进 RAII 包装、消灭裸 glDelete*；强制 early-return 路径 ASan/CRT 调试堆零泄漏 | 1·阶段2（RAII 与所有权：从裸句柄到智能指针） | M1 |
| M3 | 🧰 CMake 工程化 | 渲染器拆为库目标 + demo 应用 + CTest，FetchContent 接 glfw/assimp，新环境三步从零构建；泛型 ResourceManager 一并沉淀 | 1·阶段4（工具链工程化：CMake 构建体系）、1·阶段3（标准库与泛型编程：从用模板到写模板） | M2 |
| M4 | 💡 光照与模型加载主体完成 | 方向光/点光/聚光 + 材质贴图 + 阴影贴图；Assimp/glTF 加载、学习资产冻结；应用层无裸 GL 调用、分层收敛——本条同时是第 6 节的启动前提与 IRenderer 的对接面 | 3·阶段3（光照与材质）、3·阶段4（模型加载与渲染器分层） | M3 |
| M5 | 🔌 后端接口抽象 | 所有渲染调用收敛到 IRenderer 接口之后，换 stub 实现时游戏逻辑零改动 | 4·阶段6（场景组织、序列化与引擎分层） | M4（3·阶段4 已收拢实现，此处完成接口化） |
| M6 | ✨ 进阶渲染与现代 API 观念 | 离屏渲染 + 可级联后处理链 + 实例化；延迟管线与 PBR/IBL，扫描矩阵冻结为 P3 对照基准；DSA/AZDO（持久映射、间接绘制），达成「现代 API 观念就绪」 | 3·阶段5–6（帧缓冲/后处理/实例化；延迟渲染与 PBR/IBL）、3·阶段7（DSA 与 AZDO） | M4（M5 可并行：3·阶段5–7 不依赖接口化） |
| M7 | ⚡ compute 并入（GPU 驱动闭环） | GPU 粒子（SOA + ping-pong + 存活压缩）、compute 视锥剔除 + 间接绘制、（选做）HiZ/bloom；产出即 P4·M4–M5 | 5·阶段5（图形应用专题：并入第 3 节渲染器） | M6；并消费 P4·M0–M3 |
| M8 | 🏁 Vulkan 复刻（双后端对照） | 同一渲染器（至少到光照）迁移为 Vulkan 后端，render graph 自动补屏障，与 OpenGL 版帧结果一致；已学第 4 节则挂到 IRenderer 之后 | 6·阶段5（后端工程化与复刻迁移），铺垫见 6·阶段1–4 | 启动线为 M4；默认在 M7 后收尾（6·阶段1–4 可与第 5 节并行） |

**完成判据**：
- 双 API 对照：同一学习资产场景在 OpenGL 与 Vulkan 两条后端下帧结果一致（RenderDoc 逐 pass 对照）。
- 单一场景入口：一份场景描述驱动完整画面（模型 + 光照 + 阴影 + 相机 + 后处理），P2 与 P4 的产出均运行其上。
- 工程质量：新环境 configure→build→test 三步全绿；应用层无裸 GL/Vulkan 调用；强制 early-return 下零泄漏。
- GPU 驱动闭环：粒子/剔除路径无 CPU↔GPU 强同步点与每帧读回，Nsight 帧时间有量化记录。

**与其他项目的依赖**：P2 以 M1 的 2D 路径为地基、汇合于 M5 的 IRenderer；P3 以 M4/M6 冻结的场景为对照基准；P4 的产出在 M7 并入；M8 复用 M5 的接口与 M3 的 CMake 工程（6·阶段1 直接复用该工程）。

## 项目 P2：2D 小引擎（第 4 节项目，建于 P1 之上）

**定位与最终形态**：第 4 节各阶段的工程载体——不以画面为目标、以架构可演进为目标（第 4 节边界声明）；最终形态为固定步长循环 + 三代对象模型对照 + 事件总线 + 句柄资源管理 + JSON 场景驱动、渲染收敛于 IRenderer 的可玩小游戏（平台跳跃或太空射击），附全模式架构决策记录（ADR）。

**参与章节**：第 4 节（主体，全部七个阶段）；第 3 节提供渲染地基（3·阶段2 的 2D 路径、3·阶段4 的分层收敛）；第 1 节提供性能与并发观念（1·阶段5/6）；第 6 节提供可选 Vulkan 后端（6·阶段5）。

**里程碑表**：

| 编号 | 里程碑 | 达成内容 | 来源（章节·阶段） | 前置 |
|---|---|---|---|---|
| M0 | ⏱ 游戏循环骨架 | 「方块按帧移动」改造为固定步长 + 渲染插值，锁 30/60/144 帧下运动与物理行为一致 | 4·阶段1（游戏循环与实时架构） | P1·M1 |
| M1 | 🧩 对象模型三代演进 | 深继承 / GameObject+组件 / 自写 toy ECS 三版同题实现，profiler 对比 1000+ 实体帧耗时 | 4·阶段2（对象模型演进：继承 → 组件 → ECS） | M0 |
| M2 | 🎯 行为与输入 | 「巡逻-追击-攻击-逃跑」FSM 与行为树各一版（行为树带可视化调试）；玩家输入 Command 化 | 4·阶段3（行为决策与输入：谁做决定） | M1 |
| M3 | 📨 事件总线 | 「受伤→死亡→掉落→加分」事件化重构；订阅者销毁不崩、帧末派发可追查 | 4·阶段4（事件与消息系统：谁通知谁） | M2 |
| M4 | 🗄 资源与生命周期 | 句柄式资源管理器（纹理/网格）+ 粒子对象池；句柄失效安全降级、热路径零分配 | 4·阶段5（资源与生命周期管理） | M3 |
| M5 | 🏛 场景、序列化与分层 | 场景树 + 变换层级、JSON 关卡保存/加载整个实体-组件场景、渲染调用全部收敛到 IRenderer（与 P1·M5 同一落点） | 4·阶段6（场景组织、序列化与引擎分层） | M4、P1·M4 |
| M6 | 🎮 小引擎 1.0 | 用小引擎完成可玩小游戏；为每个用了/没用/移除的模式写一条 ADR | 4·阶段7（综合：小引擎 1.0 与模式复盘） | M5 |

**完成判据**：一个 JSON 文件即可实例化完整场景；渲染后端换 stub 实现时游戏逻辑零改动；小游戏从头到尾可玩；ADR 覆盖全部已学模式，任一模式能回答「去掉它坏什么、留着它多花什么」。

**与其他项目的依赖**：渲染地基依赖 P1·M1，接口化与 P1·M5 汇合；与 P3/P4 无直接依赖；P1·M8 之后可将 Vulkan 后端挂到 IRenderer 之后（6·阶段5 的可选项）。

## 项目 P3：软件光栅器 + CPU 路径追踪器（第 2 节 CPU 对照项目）

**定位与最终形态**：纯 CPU、不绑定任何图形 API 的两份参照实现（第 2 节边界声明）——软件光栅器复刻 GPU 光栅化全过程，路径追踪器从第一性原理解渲染方程；最终形态为能与 P1 的 OpenGL 光照与 PBR 结果逐项对照、且每处差异都能用理论解释的 CPU 参照系。

**参与章节**：第 2 节（主体，阶段 1–4、6–8）；对照基准来自第 3 节（3·阶段3 光照基准、3·阶段6 PBR 扫描矩阵，经 P1 冻结）；数学库由 1·阶段1 补齐零/三/五法则标注与单测。

**里程碑表**：

| 编号 | 里程碑 | 达成内容 | 来源（章节·阶段） | 前置 |
|---|---|---|---|---|
| M0 | 📐 手写数学库 | Vec2/3/4、Mat4 及 lookAt/ortho/perspective/viewport，每函数注明几何意义，性质单测全绿（RᵀR=I 等） | 2·阶段1–2（前置数学；变换与投影） | —（与 1·阶段1–2 并行起步） |
| M1 | 🖼 软件光栅化 | 线框 → 实心 → 深度测试 → 纹理与透视校正插值，输出图片文件；渲染与第 3 节同款模型 | 2·阶段3（光栅化：从三角形到像素） | M0 |
| M2 | 🌗 着色与阴影 | Blinn-Phong + 法线贴图 + 阴影贴图；与 P1 的光照对照基准（3·阶段3 冻结）同场景对照，用理论解释每处差异 | 2·阶段4（着色与材质：局部光照原理） | M1、P1·M4（对照基准） |
| M3 | 🔭 Whitted 光线追踪 | 光线-球/三角形（Möller–Trumbore）求交、BVH 概念、递归追踪与阴影光线，经典三球场景 | 2·阶段6（光线追踪与辐射度量学） | M0（求交基础） |
| M4 | 🎨 路径追踪 | 蒙特卡洛 + GGX 金属粗糙材质 + Cornell Box；金属度/粗糙度扫描网格对照 P1 的 PBR 基准（3·阶段6 冻结） | 2·阶段7（路径追踪与物理渲染入门） | M3、P1·M6（对照基准） |
| M5（选做） | 🌊 动画与模拟 demo | 布料/弹簧质点，显式 vs 半隐式欧拉稳定性对照 | 2·阶段8（动画与模拟概览） | M0 |

**完成判据**：光栅器正确渲染带纹理与遮挡的旋转模型；Cornell Box 能量随弹射数增加不漂移；对照 P1 的光照与 PBR 基准时每处差异都能用理论解释（把第 3 节「会用」升级为「懂原理」）；全程纯 CPU、零图形 API 依赖。

**与其他项目的依赖**：对照基准依赖 P1·M4 与 P1·M6（先冻结 OpenGL 侧结果，再在 CPU 侧复刻）；不阻塞也不依赖 P2/P4。注：按 [Topic.md](./Topic.md) 设计，CPU 路径追踪器留作第 5 节 GPU 化的实践素材，但 [Stages.md](./Stages.md) 第 5 节未列对应阶段任务，仅作可选自主延伸、不计入 P4 里程碑。

## 项目 P4：GPU 驱动渲染特性集（第 5 节产出，并入 P1）

**定位与最终形态**：第 5 节的能力载体——前四个阶段练基本功（compute / SIMT / 算法原语 / 测量），阶段 5 把成果以「GPU 粒子 + compute 剔除与间接绘制 +（选做）HiZ/bloom」的形式并入 P1；最终形态不是独立仓库，而是 P1 的 GPU 驱动渲染闭环（即 P1·M7）。

**参与章节**：第 5 节（主体，全部五个阶段）；观念前置来自第 3 节（3·阶段7 的间接绘制与显式同步）；产出落点为 P1。

**里程碑表**：

| 编号 | 里程碑 | 达成内容 | 来源（章节·阶段） | 前置 |
|---|---|---|---|---|
| M0 | ⚙️ compute 基本功 | Hello Compute、模糊 compute 化与 fragment 版逐像素一致、shared tiling 粗测收益；能画三层调用坐标映射图 | 5·阶段1（GLSL 深入与 Compute Shader 实操入口） | P1·M4（第 3 节「能读懂并使用」的起点） |
| M1 | 🧠 SIMT 与术语映射 | CUDA 复刻模糊/归约、warp 发散对比实验、shared tiling 转置带宽对照；建立 block↔workgroup 等术语对照表 | 5·阶段2（GPU 体系结构与 SIMT：CUDA + PMPP 前半） | M0 |
| M2 | 🧮 并行算法原语 | 两级 reduction、Blelloch exclusive scan、直方图、存活粒子压缩（scan + scatter）——粒子/剔除系统的核心模式 | 5·阶段3（并行算法模式：reduction / scan 及图形落地） | M1 |
| M3 | 📊 测量基线 | Nsight 基线表（耗时/有效带宽/带宽利用率/occupancy）、瓶颈分类、「问题-假设-改动-数据」四栏优化记录 | 5·阶段4（性能工程：occupancy、带宽与 Nsight 三件套） | M2 |
| M4 | 🎆 GPU 粒子系统 | 10 万+ 粒子 SOA + ping-pong + 存活压缩全在 GPU、CPU 每帧零分配 → 并入 P1 | 5·阶段5（图形应用专题：并入第 3 节渲染器） | M3、P1·M6 |
| M5 | 🚀 compute 剔除与间接绘制 | compute 视锥剔除生成可见列表 + GPU 端写间接绘制命令；（选做）HiZ 遮挡剔除或 compute bloom | 5·阶段5（图形应用专题：并入第 3 节渲染器） | M4、P1·M6（含 3·阶段7 间接绘制观念） |

**完成判据**：能向他人讲清「CPU 上传 → compute 更新 → 归约/压缩 → 间接绘制」的完整数据流；功能正确（无 ghost 粒子、剔除无漏判）且 Nsight 下帧时间有量化提升或不劣化；热路径无 CPU↔GPU 强同步点、无每帧读回。

**与其他项目的依赖**：起步依赖 P1·M4（有可运行的图形工程与着色器阅读能力）；M4/M5 依赖 P1·M6 的实例化/后处理基础与 3·阶段7 的 AZDO 观念；全部产出汇入 P1·M7；与 P2 无直接依赖。

## 项目与章节推进对照总览

双向查阅：从章节侧查「这一阶段的实践喂了哪个项目的哪个里程碑」，从项目侧查「这个里程碑来自哪一节」。读法：「阶段2→M2」表示该阶段的实践任务对应/支撑该项目的 M2；「—」表示无直接项目产出（该阶段仍服务本节知识覆盖）；「增量」指未列入里程碑编号、但持续强化该项目的工程产出。

| 章节 | P1 自写渲染器 | P2 2D 小引擎 | P3 软光栅+路径追踪 | P4 GPU 特性集 |
|---|---|---|---|---|
| 1 C++ | 阶段2→M2 RAII 化；阶段3→泛型 ResourceManager（随 M3 沉淀）；阶段4→M3 工程化；阶段5→主线程渲染+异步加载（增量）；阶段6→帧画像与零分配主循环（增量）；阶段7→clang-tidy 与重构护栏（增量） | 阶段6→ECS 数据布局性能观念的出处 | 阶段1→数学库零/三/五法则标注与单测（M0 的组成部分） | — |
| 2 图形学理论 | —（阶段2 的 NDC/深度范围差异是 3·阶段2 常见坑点的理论出处，观念层支撑） | 阶段2→场景树变换层级所消费的数学结论（M5 消费） | 阶段1–2→M0；阶段3→M1；阶段4→M2；阶段6→M3；阶段7→M4；阶段8→M5（选） | — |
| 3 OpenGL | 阶段1→M0；阶段2→M1；阶段3–4→M4；阶段5–6→M6；阶段7→M6 现代化（AZDO/持久映射/间接绘制） | 阶段2→2D quad 路径（M0 地基）；阶段4→分层收敛（M5 对接面） | 阶段3→冻结光照对照基准（供 M2）；阶段6→冻结 PBR 扫描基准（供 M4） | 阶段7→间接绘制与显式同步观念（M5 前置） |
| 4 架构 | 阶段6→M5 IRenderer 接口抽象（双后端对接面） | 阶段1→M0；阶段2→M1；阶段3→M2；阶段4→M3；阶段5→M4；阶段6→M5；阶段7→M6（小引擎 1.0） | — | — |
| 5 GPU | 阶段5→M7（compute 并入，GPU 驱动闭环） | — | —（路径追踪器 GPU 化为可选自主延伸，见 P3 依赖注记） | 阶段1→M0；阶段2→M1；阶段3→M2；阶段4→M3；阶段5→M4–M5（产出并入 P1） |
| 6 Vulkan | 阶段1–4→M8 铺垫（工程/上屏/管线与 SPIR-V/VMA，复用学习资产与 1·阶段4 工程）；阶段5→M8 复刻迁移与双后端对照；阶段6→选学特性 demo（延伸） | 阶段5→（可选）Vulkan 后端挂到 IRenderer 之后 | — | 阶段6→GPU-driven 剔除等概念衔接（选学） |

**默认相对顺序**（仅表达先后，可按需调整）：双线起步（1·阶段1–2 ∥ 2·阶段1）→ P1·M0–M2（3·阶段1–2 + 1·阶段2 RAII）→ P1·M3（1·阶段3–4）→ P1·M4（3·阶段3–4，P3 的光照对照基准就此冻结）→ P2 全线（第 4 节）与 P1·M6（3·阶段5–7）并行 → P4 全线（第 5 节）汇成 P1·M7，与 P3·M3–M4（2·阶段6–7）并行 → P1·M8（第 6 节，其阶段 1–4 铺垫可再提前）。P3·M0–M2 随第 2 节正文穿插完成，对照点分别锚定 P1·M4/M6。
