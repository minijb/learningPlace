# （领域主题标题——由用户填写）

> 本文件是领域级主题纲领文件，限定 `<文件夹名>/` 内一切知识的范围；上位框架见根纲领
> `<../Topic.md>`（相对路径以实际结构为准）。本文件声明的范围必须包含于父级划定范围之内。
> 标题（# 与 ##）由用户管理，Agent 只在标题之下填充纲领性要点并提出建议。
> 每节 3–6 条、每条一句话，只放本领域内子方向级的方向性要点，不放实现细节；
> 疑似超出本领域范围的内容，先与用户确认归属再处理。

## （子方向 1——由用户填写）

<!-- Agent 将在此填充纲领性要点；填充前会先与你确认细节与作用域归属 -->

## （子方向 2——由用户填写）

<!-- 同上 -->
